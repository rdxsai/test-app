# ============================================================================
# Docker Environment Configuration Template
# ============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp .env.docker.template .env
#
# IMPORTANT: Never commit .env file to git (it's in .gitignore)
# ============================================================================

# ============================================================================
# AZURE OPENAI CONFIGURATION (Required for AI features)
# ============================================================================
# Get these from Azure Portal -> Azure OpenAI Service
AZURE_OPENAI_ENDPOINT=https://your-endpoint.azure-api.net
AZURE_OPENAI_DEPLOYMENT_ID=gpt-4
AZURE_OPENAI_API_VERSION=2024-02-15-preview
AZURE_OPENAI_SUBSCRIPTION_KEY=your-subscription-key-here

# ============================================================================
# CANVAS LMS CONFIGURATION (Required for Canvas integration)
# ============================================================================
# Get API token from Canvas -> Account -> Settings -> New Access Token
CANVAS_BASE_URL=https://canvas.vt.edu
CANVAS_API_TOKEN=your-canvas-api-token-here
COURSE_ID=your-course-id
QUIZ_ID=your-quiz-id

# ============================================================================
# SERVICE CONFIGURATION (Already configured in docker-compose.yml)
# ============================================================================
# These are set automatically by Docker Compose, but you can override them here
# if needed for local development outside Docker

# Ollama Configuration
# OLLAMA_HOST=http://ollama:11434
# OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# PostgreSQL Configuration (set automatically by Docker Compose)
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
# POSTGRES_DB=socratic_tutor
# POSTGRES_USER=app_user
# POSTGRES_PASSWORD=changeme_dev

# Schema isolation: "prod" for supervisor's curated data, "dev" for test data
# DB_SCHEMA=prod

# ============================================================================
# EXAMPLE VALUES (for reference)
# ============================================================================
# AZURE_OPENAI_ENDPOINT=https://itls-openai-connect.azure-api.net
# AZURE_OPENAI_DEPLOYMENT_ID=gpt-4
# CANVAS_BASE_URL=https://canvas.vt.edu
# CANVAS_API_TOKEN=1234~abcdefghijklmnopqrstuvwxyz
# COURSE_ID=220959
# QUIZ_ID=123456

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
# 1. Copy this file: cp .env.docker.template .env
# 2. Fill in your Azure OpenAI credentials
# 3. Fill in your Canvas LMS credentials
# 4. Save the file
# 5. Run: docker-compose up --build
# 6. Access the app at http://localhost:8080
# ============================================================================
